import SwiftUI

extension Color {
    // Defines a fallback for Color.fox so the splash screen compiles
    // You should define this color in your Assets.xcassets later
    // static let fox = Color("fox", bundle: nil) // Conflicts with iOS 17+ Asset Symbols
}
